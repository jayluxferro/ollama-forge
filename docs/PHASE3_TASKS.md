# Phase 3 — Abliterated models (refusal removal): sub-tasks

| # | Task | Status |
|---|------|--------|
| 3.1 | **ABLITERATE doc** — Pipeline, Sumandora reference, layer access, export to Ollama | Done |
| 3.2 | **Integrate/wrap** — compute-dir CLI (optional deps: uv sync --extra abliterate) | Done |
| 3.3 | **Pipeline + re-import** — Doc convert step (GGUF → ollama create) | Done |

**Deliverables:** Reproducible abliterated build (doc + optional tooling); doc on limitations and supported architectures.
