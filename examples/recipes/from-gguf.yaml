# Example: create an Ollama model from a GGUF file (e.g. after fetch or manual download).
# Copy this file, set gguf to your .gguf path, then: uv run ollama-forge build <file>
# Copy and set gguf path, then build.

name: my-model
gguf: /path/to/your-model.gguf
temperature: 0.6
num_ctx: 8192
